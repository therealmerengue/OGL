#include "Coordinates.h"

Coordinates::Coordinates(glm::vec3 position, glm::vec3 size, glm::vec3 rotationAxis, GLfloat rotationAngle) :
	position(position), size(size), rotationAxis(rotationAxis), rotationAngle(rotationAngle)
{
}
